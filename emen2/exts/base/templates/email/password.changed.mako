From: ${from_addr}
To: ${to_addr}
Cc: ${from_addr}
Subject: ${TITLE} password changed

${to_addr}:

The password for your account on ${TITLE} has been changed.

If you did not change your password, please contact the ${TITLE} administrator, ${from_addr}

Thankyou,

${TITLE} Administrator
${from_addr}
${uri}